import 'package:flutter/material.dart';

class BataillePage extends StatelessWidget {
  const BataillePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bataille'),
      ),
      body: const Center(
        child: Text('Bataille Game Coming Soon'),
      ),
    );
  }
}
